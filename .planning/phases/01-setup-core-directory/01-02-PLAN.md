---
phase: 01-setup-core-directory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/MainLayout.tsx
  - src/lib/utils.ts
  - components.json
autonomous: true

must_haves:
  truths:
    - "Site displays consistent header and footer across all pages"
    - "Design system matches NextGen Preservation Collab branding"
    - "Layout is responsive on mobile, tablet, and desktop"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Site header with navigation"
      min_lines: 30
    - path: "src/components/layout/Footer.tsx"
      provides: "Site footer with contact info"
      min_lines: 20
    - path: "tailwind.config.ts"
      provides: "Tailwind configuration with brand fonts and colors"
      contains: "Oswald"
  key_links:
    - from: "src/components/layout/MainLayout.tsx"
      to: "Header.tsx and Footer.tsx"
      via: "Component composition"
      pattern: "<Header.*<Footer"
---

<objective>
Establish the design system and layout structure using Tailwind v4 and shadcn/ui with NextGen Preservation Collab branding.

Purpose: Create the visual foundation and reusable components that ensure consistent, professional design across all pages.
Output: Configured design system with Header, Footer, and MainLayout components ready for page integration.
</objective>

<execution_context>
@/Users/vincecain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincecain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vincecain/Projects/nextgen-preservation-directory/.planning/PROJECT.md
@/Users/vincecain/Projects/nextgen-preservation-directory/.planning/ROADMAP.md
@/Users/vincecain/Projects/nextgen-preservation-directory/.planning/STATE.md
@/Users/vincecain/Projects/nextgen-preservation-directory/.planning/phases/01-setup-core-directory/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Setup Tailwind v4 and shadcn/ui with brand configuration</name>
  <files>
    tailwind.config.ts
    src/app/globals.css
    src/lib/utils.ts
    components.json
  </files>
  <action>
    Initialize shadcn/ui using `npx shadcn@latest init`:
    - Use Tailwind CSS
    - Install base components (Button, Card, Input will be needed later)
    - Create src/lib/utils.ts with cn() helper (clsx + tailwind-merge)

    Configure tailwind.config.ts with NextGen Preservation Collab branding:
    - Fonts: Add Oswald (headings), Lato (body), Inter (fallback) via Google Fonts
    - Colors: Dark theme palette (use Tailwind slate/zinc as base, customize if brand colors provided)
    - Extend defaults for professional directory aesthetic

    Update src/app/globals.css:
    - Import Tailwind base, components, utilities
    - Add custom CSS variables for brand colors if needed
    - Set global font families

    Per research: Tailwind v4 stable in 2026, shadcn/ui provides copy-paste components with full ownership. Design context specifies Oswald, Lato, Inter fonts with dark backgrounds.
  </action>
  <verify>
    Run `npm run dev` and inspect http://localhost:3000 to see Tailwind styles applied
    Check tailwind.config.ts contains Oswald and Lato font families
    Verify src/lib/utils.ts exports cn() function
  </verify>
  <done>
    Tailwind v4 configured with brand fonts and colors, shadcn/ui initialized, cn() utility available
  </done>
</task>

<task type="auto">
  <name>Create Header and Footer layout components</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
  </files>
  <action>
    Create src/components/layout/Header.tsx:
    - Site title: "NextGen Preservation Collab Directory"
    - Navigation links: Home, Browse, Map (Phase 1 pages)
    - Responsive: Mobile hamburger menu (use shadcn/ui Sheet component if complex, or simple details/summary for MVP)
    - Styling: Dark background with white text per brand guidelines
    - Include max-width container for content alignment

    Create src/components/layout/Footer.tsx:
    - Contact email: nextgenpreservationcollab@gmail.com
    - Instagram link: @nextgenpreservationcollab
    - Copyright: "© 2026 NextGen Preservation Collab"
    - Physical address: 427 W Muhammad Ali Blvd, Suite 200, Louisville, KY
    - Simple, clean layout (no complex multi-column for MVP)
    - Responsive: Stack on mobile, row on desktop

    Per project context: Clean, modern, professional design. Photography-forward with dark backgrounds and white logo treatment.
  </action>
  <verify>
    Create temporary page importing Header and Footer to visually inspect
    Check responsive behavior by resizing browser window
    Verify all contact information and links are correct
  </verify>
  <done>
    Header displays site title and navigation, Footer displays contact info and social links, both responsive
  </done>
</task>

<task type="auto">
  <name>Create MainLayout component wrapper</name>
  <files>
    src/components/layout/MainLayout.tsx
  </files>
  <action>
    Create src/components/layout/MainLayout.tsx:
    - Accept children prop (React.ReactNode)
    - Compose Header, main content area, and Footer
    - Main content area: Use semantic `<main>` tag with min-height to push footer to bottom
    - Apply max-width container with padding for consistent spacing
    - Ensure proper ARIA landmarks for accessibility (header, main, footer)

    Structure:
    ```tsx
    <div className="flex min-h-screen flex-col">
      <Header />
      <main className="flex-1 container mx-auto px-4 py-8">
        {children}
      </main>
      <Footer />
    </div>
    ```

    Per research: Semantic HTML and ARIA landmarks required for WCAG 2.1 AA compliance (UX-03 success criterion).
  </action>
  <verify>
    Import MainLayout in app/layout.tsx or test page
    Verify Header stays at top, Footer at bottom, content fills space
    Check that footer doesn't float when content is short
  </verify>
  <done>
    MainLayout component created with Header, content area, and Footer composition, footer properly positioned
  </done>
</task>

</tasks>

<verification>
1. Tailwind v4 configuration includes Oswald and Lato fonts
2. Header component displays with navigation and dark background
3. Footer component displays with contact info and social links
4. MainLayout wraps content with proper layout structure
5. Design is responsive across mobile, tablet, desktop viewports
</verification>

<success_criteria>
- Tailwind v4 configured with NextGen Preservation Collab brand fonts and colors
- shadcn/ui initialized with utility function available
- Header component created with navigation
- Footer component created with contact information
- MainLayout component wraps Header, content, and Footer with responsive design
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-core-directory/01-02-SUMMARY.md`
</output>
